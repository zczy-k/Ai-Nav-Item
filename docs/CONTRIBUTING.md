# 贡献指南

感谢你对 Con-Nav-Item 项目的关注！我们欢迎各种形式的贡献。

## 🤝 如何贡献

### 报告问题

- 在 [Issues](https://github.com/zczy-k/Con-Nav-Item/issues) 页面创建新问题
- 详细描述问题和复现步骤
- 提供系统环境信息（操作系统、Node.js 版本等）

### 提交功能请求

- 在 Issues 页面创建功能请求
- 详细描述功能需求和使用场景
- 说明为什么这个功能对用户有价值

### 代码贡献

1. **Fork 项目**
   ```bash
   git clone https://github.com/your-username/Con-Nav-Item.git
   cd Con-Nav-Item
   ```

2. **创建功能分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **开发和测试**
   ```bash
   npm run install:all
   npm run dev
   ```

4. **提交代码**
   ```bash
   git add .
   git commit -m "feat: add your feature description"
   git push origin feature/your-feature-name
   ```

5. **创建 Pull Request**

## 📝 代码规范

### 提交信息格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 格式：

```
<type>(<scope>): <description>

[optional body]

[optional footer(s)]
```

**类型 (type):**
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

**示例:**
```
feat(ai): add DeepSeek AI provider support
fix(backup): resolve WebDAV connection timeout issue
docs(readme): update installation instructions
```

### JavaScript 代码规范

- 使用 2 空格缩进
- 使用单引号
- 行末不加分号
- 函数和变量使用驼峰命名
- 常量使用大写下划线命名

### Vue 代码规范

- 组件名使用 PascalCase
- Props 使用 camelCase
- 事件名使用 kebab-case
- 模板中使用 kebab-case

## 🧪 测试

在提交 PR 前，请确保：

1. **功能测试**
   - 新功能正常工作
   - 不影响现有功能
   - 在不同浏览器中测试

2. **API 测试**
   - 测试相关 API 端点
   - 验证错误处理
   - 检查响应格式

3. **扩展测试**（如果涉及）
   - 在 Chrome/Edge 中测试
   - 在 Firefox 中测试
   - 验证权限和功能

## 📚 开发环境设置

### 后端开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run server

# 检查密码工具
npm run check-password check
```

### 前端开发

```bash
# 进入前端目录
cd web

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

### 浏览器扩展开发

1. 修改 `browser-extension/` 目录下的文件
2. 在浏览器中加载解压的扩展程序
3. 测试功能
4. 更新 `manifest.json` 版本号

## 🔍 代码审查

PR 将会被审查以确保：

- 代码质量和可读性
- 功能完整性和正确性
- 安全性考虑
- 性能影响
- 文档完整性

## 📖 文档贡献

- 更新 README.md
- 添加功能文档到 `docs/features/`
- 更新部署文档到 `docs/deployment/`
- 添加 API 文档

## 🎯 开发优先级

当前需要帮助的领域：

1. **测试覆盖** - 添加单元测试和集成测试
2. **性能优化** - 数据库查询优化、前端性能
3. **国际化** - 多语言支持
4. **移动端优化** - 响应式设计改进
5. **AI 功能** - 新的 AI 提供商支持

## 📞 联系方式

- GitHub Issues: [项目问题页面](https://github.com/zczy-k/Con-Nav-Item/issues)
- 邮箱: 通过 GitHub 联系

感谢你的贡献！🎉